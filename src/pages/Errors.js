import Button from '@mui/material/Button';
import { Link } from "react-router-dom";
const Errors = () => {

  return (<div>
    <h1>Working on Errors Component</h1>
     <Link to="/"><Button size="small" color="primary">Go Back</Button></Link>
    </div>);
};

export default Errors;