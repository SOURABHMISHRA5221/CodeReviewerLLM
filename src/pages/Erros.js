import Button from '@mui/material/Button';
import { Link } from "react-router-dom";
const Erros = () => {

  return (<div>
    <h1>Working on Erros Component</h1>
     <Link to="/"><Button size="small" color="primary">Go Back</Button></Link>
    </div>);
};

export default Erros;